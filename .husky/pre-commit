export PATH="/opt/homebrew/bin:$PATH"
export PATH="node_modules/.bin:$PATH"

lint-staged && prettier --check . && mocha --ui tdd --require ts-node/register --require src/test/unit/setup.ts 'src/test/unit/**/*.test.ts'
